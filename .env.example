# Copy to ~/.config/ultrawhisper/.env to enable cleanup mode (Cmd+Opt+Shift+Space).
# Only ONE key is needed. With no .env, cleanup mode just pastes the raw transcript.
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# Optional model override (default: claude-haiku-4-5-20251001 / gpt-4o-mini)
# LLM_MODEL=
